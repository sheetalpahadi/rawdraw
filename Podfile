# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RawDraw' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RawDraw
  pod 'Firebase'
	pod 'FirebaseAuth'
	pod 'FirebaseFirestore'
  pod 'FirebaseStorage'

  target 'RawDrawTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RawDrawUITests' do
    # Pods for testing
  end

end
